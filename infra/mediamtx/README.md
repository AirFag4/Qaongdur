# mediamtx

Media relay configuration for RTSP, WebRTC, and HLS.

MediaMTX is part of the planned `core` runtime and will be used for stream relay in both external-NVR and camera-direct local NVR paths.
