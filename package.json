{
  "name": "qaongdur",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.1",
  "scripts": {
    "setup": "pnpm install",
    "dev": "pnpm --filter @qaongdur/web dev",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "docker-up": "sh -c 'ENV_FILE=infra/docker/.env; if [ ! -f \"$ENV_FILE\" ]; then ENV_FILE=infra/docker/.env.example; fi; docker compose --env-file \"$ENV_FILE\" -f infra/docker/compose.auth.yml up -d'"
  }
}
